package Game;

import javax.swing.JFrame;

public class GameSlotsMenu extends JFrame {
  public GameSlotsMenu() {
    setTitle("Game Slots Menu");
    setSize(300, 200);  // Set appropriate size for the window
    setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
    setLocationRelativeTo(null); // Center the frame on the screen

    // Add a simple label or other components to indicate that this is the GameSlotsMenu
//    JLabel label = new JLabel("Game Slots Menu", JLabel.CENTER);
//    add(label, BorderLayout.CENTER);
  }
}
